# Bloc `map` obligatoirement au niveau http{} : il alimente la variable
# $connection_upgrade utilisée par le vhost pour les websockets de Next.
# Installé dans /etc/nginx/conf.d/ par scripts/setup_vps.sh.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
